{{#if isEnvArray}}
  {{page-nav
    list=message.env
    resetPositionOnChange=message.key
    position=currentEnvPosition
    extraClasses="env-nav"
    navigate=envChangedAction}}
{{/if}}
{{{html}}}
